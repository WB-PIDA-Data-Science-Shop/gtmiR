% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/corr_plot_funs.R
\name{make_corr_boxplot}
\alias{make_corr_boxplot}
\title{a sina-style jittered dot layer coloured by the grouping variable, and a
mean diamond overlay. No background band rectangles are used — the colour
palette is applied directly to the dot and mean layers for cleaner integration.}
\usage{
make_corr_boxplot(
  data,
  x,
  color_by = "income_group",
  facet_by = NULL,
  style = c("boxplot", "violin", "both"),
  title = NULL,
  filename = NULL
)
}
\arguments{
\item{data}{A data frame. Typically \code{corr_base} from the correlation
analysis pipeline, already filtered or passed in full.}

\item{x}{A string naming the GTMI index column to plot on the x-axis
(\code{"gtmi"}, \code{"cgsi"}, \code{"psdi"}, \code{"dcei"}, \code{"gtei"}).}

\item{color_by}{A string naming the column used for point and mean colour.
Supported values: \code{"region"}, \code{"income_group"}, \code{"grp"}.
Defaults to \code{"income_group"}.}

\item{facet_by}{A string naming the column to use for \code{facet_wrap},
or \code{NULL} (default) for no faceting.}

\item{style}{One of \code{"boxplot"} (default, notched), \code{"violin"},
or \code{"both"} (violin with boxplot inset).}

\item{title}{A string for the plot title. Auto-generated if \code{NULL}.}

\item{filename}{A string file path to save the plot, or \code{NULL}
(default) to return without saving.}
}
\value{
A \code{ggplot} object.
}
\description{
a sina-style jittered dot layer coloured by the grouping variable, and a
mean diamond overlay. No background band rectangles are used — the colour
palette is applied directly to the dot and mean layers for cleaner integration.
}
\examples{
\dontrun{
make_corr_boxplot(
  data     = corr_base,
  x        = "gtmi",
  color_by = "income_group",
  facet_by = "outcome",
  style    = "both"
)
}

}
